package org.unibuc.persistance.repository;

import org.unibuc.persistance.model.BankAccount;
import org.unibuc.persistance.repository.base.BaseRepository;

public interface BankAccountRepository extends BaseRepository<BankAccount, Long, String> {
}
