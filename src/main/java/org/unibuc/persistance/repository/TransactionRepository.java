package org.unibuc.persistance.repository;

import org.unibuc.persistance.model.Transaction;
import org.unibuc.persistance.repository.base.BaseRepository;

public interface TransactionRepository extends BaseRepository<Transaction, Long, String> {
}
