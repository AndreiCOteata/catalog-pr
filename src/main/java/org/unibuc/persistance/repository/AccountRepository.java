package org.unibuc.persistance.repository;

import org.unibuc.persistance.model.Account;
import org.unibuc.persistance.repository.base.BaseRepository;

public interface AccountRepository extends BaseRepository<Account, Long, String> {
}
