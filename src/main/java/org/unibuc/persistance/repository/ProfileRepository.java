package org.unibuc.persistance.repository;

import org.unibuc.persistance.model.Profile;
import org.unibuc.persistance.repository.base.BaseRepository;

public interface ProfileRepository extends BaseRepository<Profile, Long, String> {
}
