package org.unibuc.persistance.repository;

import org.unibuc.persistance.model.Card;
import org.unibuc.persistance.repository.base.BaseRepository;

public interface CardRepository extends BaseRepository<Card, Long, String> {
}
