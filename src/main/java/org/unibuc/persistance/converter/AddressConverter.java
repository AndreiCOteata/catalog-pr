package org.unibuc.persistance.converter;

import org.unibuc.persistance.converter.base.BaseConverter;
import org.unibuc.persistance.dto.AddressDto;
import org.unibuc.persistance.model.Address;

public interface AddressConverter extends BaseConverter<Address, AddressDto> {
}
