package org.unibuc.persistance.converter;

import org.unibuc.persistance.converter.base.BaseConverter;
import org.unibuc.persistance.dto.BranchDto;
import org.unibuc.persistance.model.Branch;

public interface BranchConverter extends BaseConverter<Branch, BranchDto> {
}
