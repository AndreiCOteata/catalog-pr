package org.unibuc.persistance.converter;

import org.unibuc.persistance.converter.base.BaseConverter;
import org.unibuc.persistance.dto.BankAccountDto;
import org.unibuc.persistance.model.BankAccount;

public interface BankAccountConverter extends BaseConverter<BankAccount, BankAccountDto> {
}
